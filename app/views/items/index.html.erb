<h1>Items#index</h1>
<p>Find me in app/views/items/index.html.erb</p>

<h1>購入商品一覧</h1>
<% if user_signed_in? %>
  <% @items.each do |item| %>
    <p><%= item.name %> : <%= item.price %></p>
  <% end %>
<% else %>
  <p>ログインしてください</p>
<% end %>